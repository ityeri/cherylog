[project]
name = "cherylog"
version = "0.1.0"
description = "Universal log bot (hopefully)"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "discord>=2.3.2",
    "dotenv>=0.9.9",
]
